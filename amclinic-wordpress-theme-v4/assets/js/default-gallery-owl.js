jQuery(document).ready(function ($) {
  // default wp gallery
  $('.owl-carousel').owlCarousel({
    loop: true,
    margin: 10,
    nav: true,
    dots: true,
    center: true,
    items: 1,
    autoHeight: true,
  });
});
